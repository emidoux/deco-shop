/**
 * AppContext establece la constante que ejecuta la creacion de un contexto de React.
 */

import React from "react";

const AppContext = React.createContext({});

export default AppContext;